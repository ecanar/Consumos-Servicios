[build]
  command = "npm run build"
  publish = "dist"

# Redirección inteligente para que las rutas internas de React (Single Page App) funcionen al recargar
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
