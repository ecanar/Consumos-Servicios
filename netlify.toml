[build]
  # Indicarle a Netlify que el proyecto real de React está en la subcarpeta frontend
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Redirección inteligente para rutas de React
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
